# Note: In production, use a proper secret management solution
# This is a template - replace with actual secrets

apiVersion: v1
kind: Secret
metadata:
  name: db-viewer-secrets
  namespace: db-viewer
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:password@metadata-postgres:5432/db_viewer"
  SANDBOX_PASSWORD: "postgres"
