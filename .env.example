# PgDumpLens Environment Variables
# Copy this file to .env and update values as needed

# ===========================================
# Database Passwords (REQUIRED for production)
# ===========================================
# Generate strong passwords: openssl rand -base64 32
DB_PASSWORD=change_me_to_secure_password
SANDBOX_PASSWORD=change_me_to_secure_password

# ===========================================
# Network Configuration
# ===========================================
# HTTP port for Nginx reverse proxy
HTTP_PORT=80

# HTTPS port (if SSL is configured)
# HTTPS_PORT=443

# ===========================================
# Application Settings
# ===========================================
# Log level: trace, debug, info, warn, error
RUST_LOG=info

# Default TTL for uploaded dumps (in days, default: 7)
TTL_DAYS=7

# Worker poll interval (in seconds)
WORKER_POLL_INTERVAL_SECS=5

# Cleanup interval - how often to check for expired dumps (in seconds, default: 1 hour)
CLEANUP_INTERVAL_SECS=3600

# ===========================================
# Storage
# ===========================================
# Directory where dump files are stored (inside container)
DUMP_STORAGE_PATH=/dumps
